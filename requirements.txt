# DRO-FairML dependencies
# Lower bounds are the minimums the code was written against. Upper bounds avoid
# accidental major-version breaks. Compatible with Python >=3.10 (setup.py).
#
# Verified working environment (2026-08-04, macOS, Python 3.14):
#   torch 2.11.0, numpy 2.4.4, pandas 3.0.3, scikit-learn 1.8.0, scipy 1.17.1,
#   matplotlib 3.10.8, seaborn 0.13.2, tqdm 4.67.3, pytest 9.0.3,
#   xlrd 2.0.2, openpyxl 3.1.5, PyYAML 6.0.3
#
# Install:  python3 -m pip install -r requirements.txt
# Optional: tectonic (LaTeX) for `make paper` / `make report` — install via
#   brew install tectonic   (or https://tectonic-typesetting.github.io/)

torch>=2.0.0,<3
numpy>=1.24.0,<3
pandas>=2.0.0,<4
scikit-learn>=1.3.0,<2
scipy>=1.11.0,<2
matplotlib>=3.7.0,<4
seaborn>=0.13.0,<1
tqdm>=4.65.0,<5
pytest>=7.4.0,<10
xlrd>=2.0.0,<3
openpyxl>=3.1.0,<4
pyyaml>=6.0.0,<7
