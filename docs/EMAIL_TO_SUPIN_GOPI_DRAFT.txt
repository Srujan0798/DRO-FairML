Subject: Request for GPU account on flair2.iitgn.ac.in for UTKFace DRO-FairML experiments (IITGN, under Prof. Manisha Padala)

Dear Supin,

I am Rapuru Ganesh (23110271), working on the DRO-FairML project under Prof. Manisha Padala (with Kuldeep).

Per our advisor's request on May 19, we need to run the UTKFace image experiments on the server. Setup:
- Dataset: ~24k UTKFace images → ResNet18 512-dim features, gender as binary protected attribute (y = gender prediction task).
- Attacks: FairnessTargetedPGD (modes: dp, if, combined) — the "adversarial PGD" fairness-targeted corruption.
- Canonical config (matching our verified tabular headline results): K_inner=10, tau=1.0 (fixed, not temperature schedule), 6 seeds, alphas in {0.0, 0.1, 0.2, 0.3, 0.4}, epochs=60, pgd_steps=20.
- Goal: produce the fairness_pgd results bucket under the same provenance contract as the tabular canonical run (k_inner/tau/pgd_steps/epochs/n_seeds_planned etc. recorded on every row).

Local proof-of-concept (CPU smoke, canonical config):
- Successfully executed with exact K_inner=10 + tau=1 + FairnessTargetedPGD + provenance.
- 2 rows present in results/utkface_all_results.json under "fairness_pgd" (schema-validated against current runner).
- Full server script hardened and documented: experiments/run_utkface_server.py (supports --attack, --alphas, --n_seeds=6, --tau 1.0 --k_inner 10, device=cuda; resume-safe per-attack JSONs; --help includes canonical examples).

We are ready to execute immediately once we have access. Previous connection attempts from our side hit SSL/DNS blocks; we require a proper user account on flair2.iitgn.ac.in (we can supply SSH public key).

Exact ready-to-run commands we will use (nohup + tmux variants; see also UTKFACE_SERVER_COMMANDS.txt in repo):

# Full canonical grid (one attack per invocation, 6 seeds, tau=1 fixed)
nohup venv/bin/python3 experiments/run_utkface_server.py \
  --attack dp --alphas 0.0 0.1 0.2 0.3 0.4 --n_seeds 6 \
  --tau 1.0 --k_inner 10 --epochs 60 --pgd_steps 20 --device cuda \
  > logs/utkface_dp_server.log 2>&1 &

# (repeat for --attack if and --attack combined; or run all three in one go)

# tmux (recommended for long sessions):
tmux new-session -d -s utk_dp 'cd /data/srujan.sai/DRO-FairML && \
  venv/bin/python3 experiments/run_utkface_server.py \
    --attack dp --alphas 0.0 0.1 0.2 0.3 0.4 --n_seeds 6 \
    --tau 1.0 --k_inner 10 --epochs 60 --pgd_steps 20 --device cuda \
    > logs/utkface_dp_server.log 2>&1'

# Quick smoke validation (CPU):
python3 experiments/run_utkface_server.py --attack dp --n_seeds 1 --alphas 0.2 --tau 1.0 --k_inner 3 --epochs 5 --pgd_steps 3 --device cpu

We will only run the lightweight feature-space FPGD experiments (no pixel-level PGD unless explicitly requested). We can use a pre-existing /data/srujan.sai/utkface_features.npz cache or re-extract if needed.

Could you please provision an account (or grant access + quota) on flair2.iitgn.ac.in? Happy to provide any additional details (public key, expected runtime, conda/venv setup notes).

Thank you in advance — this unblocks the final image experiments for the project.

Best regards,
Rapuru Ganesh (23110271)
Under Prof. Manisha Padala
CC: Kuldeep, Manisha Padala

(Repo: DRO-FairML; local smoke + server script validated 2026-06-16)
