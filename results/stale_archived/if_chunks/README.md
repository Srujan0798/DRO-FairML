# IF-attack intermediate shards (archived)

The 18 `_if_chunk_*.json` files were intermediate outputs from the local IF
parallel sweep. Their content is fully merged into committed
`results/canonical_tau1.json` (180 IF-attack rows of the 540-grid).

Shards removed from the working tree 2026-08-04 to shrink the repo; recoverable
from git history (`git log -- results/stale_archived/if_chunks/`).
**Do not use for claims** — only `canonical_tau1.json`.
