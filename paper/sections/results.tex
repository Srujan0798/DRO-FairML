\subsection{Tabular results (fixed $\tau=1$)}
\label{sec:results-tabular}

\textbf{Headline (verified; canonical $\tau{=}1$, $k_{\mathrm{inner}}{=}10$, 6-seed grid).}
At $\alpha\le0.2$, DRO-FAIR achieves \emph{significantly} lower DP than
Naive-FAIR on \textbf{Adult and Credit} under the DP-targeted and Combined
PGD attacks (Wilcoxon one-sided $p{<}0.05$, $n{=}6$ seeds, $0$--$5$;
Adult/DP is $5/6$ at $\alpha{=}0.1$ and $6/6$ otherwise),
reproducing the original Jun~30 report for Adult DP.  Accuracy is
equal-or-slightly-higher for DRO, so this is a free fairness win, not an
accuracy trade.  At $\alpha\ge0.3$ both methods fall below the
constant-predictor baseline on \textbf{Adult and Credit} (LSAC under the
DP attack stays pinned \emph{at} majority accuracy, not below), so we make
no method claim there.  The full attack grid is complete
(\textbf{540 rows}: DP+IF+Combined $\times$ 3 datasets $\times$ 5 $\alpha$
$\times$ 6 seeds $\times$ 2 methods).  The IF-attack third is
\emph{mixed} (not a three-attack sweep): Adult/Credit $\alpha\le0.2$ still
support DRO on DP under IF attack; LSAC/IF and Adult IF $\alpha\ge0.3$ do
not (see Q7 below and
\texttt{results/if\_wilcoxon\_summary.txt}).  Earlier IF metrics were
degenerate ($\approx10^{-10}$) before the cosine fix in
\texttt{src/evaluation/metrics.py}.  Full per-seed traces are in
\texttt{results/canonical\_tau1.json}.

\paragraph{$\alpha{=}0$ is not an attack cell.}
At $\alpha{=}0$ there is no corruption: DRO and Naive optimise
\emph{different} objectives (tilted risk / dual reweighting vs.\ fixed-Lagrange
BCE), so equal performance is not expected.  We report $\alpha{=}0$ for
completeness but exclude it from the attack-robustness headline
($\alpha\in\{0.1,0.2\}$ on Adult/Credit under DP and Combined).

\begin{figure}[t]
\centering
\includegraphics[width=0.92\textwidth]{../figures/fig_tau1_headline.pdf}
\caption{Adult, DP attack: clean DP vs.\ corruption budget $\alpha$ under
fixed $\tau{=}1$ (canonical 540-row grid, $n{=}6$ seeds).  DRO is lower
than Naive at every $\alpha$; win counts on the plot include the honest
\textbf{$5/6$} cell at $\alpha{=}0.1$ ($p{=}0.031$).  The
constant-predictor accuracy baseline for Adult is $\approx0.7521$; the
defensible accuracy regime remains $\alpha\le0.2$ (both methods fall
below the constant-predictor line at $\alpha\ge0.3$).}
\label{fig:tau1-headline}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.92\textwidth]{../figures/fig_final_wilcoxon_table.pdf}
\caption{DP-attack Wilcoxon win summary across datasets and $\alpha$
(seed-paired, one-sided $H_1$: Naive DP $>$ DRO DP; $n{=}6$).
Adult and Credit support DRO; LSAC/DP is a \textbf{degenerate loss}
(constant-predictor collapse at the LSAC majority rate $\approx0.9016$).
Constant-predictor accuracy baselines: Adult $\approx0.7521$, Credit
$\approx0.7788$, LSAC $\approx0.9016$.}
\label{fig:wilcoxon-matrix}
\end{figure}

\paragraph{Why the previous story is wrong.}
Earlier results in this project used a stepped
$\tau$-schedule: $\tau{=}100$ for $\alpha\le0.3$, $\tau{=}1$ for
$\alpha{=}0.4$.  Under that schedule DRO \emph{lost} on Adult DP at
$\alpha{=}0.1$--$0.3$ (e.g.\ $\alpha{=}0.2$: Naive $0.327147$ vs DRO
$0.503047$, from the stepped-schedule pilot, $n{=}3$).  Switching to a single fixed $\tau{=}1$ for
all $\alpha$ (per Kuldeep's Q12) flips the verdict: DRO now wins on
DP at $\alpha\le0.2$ (no method claim is made at $\alpha\ge0.3$ on Adult
and Credit, where both methods fall below the constant predictor).  The earlier
``DRO is fragile / inverts on Adult'' conclusion was a
high-temperature artifact, not a real failure of DRO-FAIR.
Table~\ref{tab:tau-comparison} makes the contrast explicit.

\begin{table}[h]
\centering\small
\caption{Adult, DP attack: the ``DRO is fragile'' story was a
$\tau{=}100$ artifact.  Same hyperparameters, only
\texttt{get\_temperature()} monkey-patched to return a constant.
\textbf{$\tau{=}1$ rows:} means over the full 6-seed canonical grid
(\texttt{results/canonical\_tau1.json}, seeds $0$--$5$).
\textbf{$\tau{=}10,100$ rows:} historical stepped-schedule pilot (seeds $0$--$2$, $n{=}3$).}
\label{tab:tau-comparison}
\rowcolors{2}{gray!8}{white}
\begin{tabular}{cc ccc cc}
\toprule
\rowcolor{hdrblue!90}
\color{white}$\alpha$ & \color{white}$\tau$ &
\color{white}\textbf{Naive DP} & \color{white}\textbf{DRO DP} &
\color{white}\textbf{$\Delta$acc} &
\color{white}\textbf{DRO wins} &
\color{white}\textbf{Verdict} \\
\midrule
0.1 & 1   & 0.2026 & \cellcolor{wingreen}\textbf{0.1999} & $+$0.001 & 5/6 & DRO\;\checkmark \\
0.1 & 10  & 0.1850 & \cellcolor{lossred}0.2231            & $+$0.001 & 0/3 & Tie \\
0.1 & 100 & 0.1801 & \cellcolor{lossred}0.2033            & $+$0.002 & 0/3 & Tie \\
\midrule
0.2 & 1   & 0.2452 & \cellcolor{wingreen}\textbf{0.2334} & $+$0.003 & 6/6 & DRO\;\checkmark \\
0.2 & 10  & 0.3382 & \cellcolor{lossred}0.4634            & $-$0.026 & 0/3 & Naive\;\checkmark \\
0.2 & 100 & 0.3271 & \cellcolor{lossred}0.5030            & $-$0.025 & 0/3 & Naive\;\checkmark \\
\midrule
0.3 & 1   & 0.2848 & \cellcolor{wingreen}\textbf{0.2614} & $+$0.009 & 6/6 & DRO\;\checkmark \\
0.3 & 10  & 0.5253 & \cellcolor{lossred}0.5532            & $+$0.010 & 0/3 & Naive\;\checkmark \\
0.3 & 100 & 0.5313 & \cellcolor{lossred}0.5622            & $+$0.012 & 0/3 & Naive\;\checkmark \\
\midrule
0.4 & 1   & 0.3140 & \cellcolor{wingreen}\textbf{0.2855} & $+$0.010 & 6/6 & DRO\;\checkmark \\
0.4 & 10  & 0.5158 & \cellcolor{lossred}0.5231            & $+$0.012 & 0/3 & Naive\;\checkmark \\
0.4 & 100 & 0.5129 & \cellcolor{lossred}0.5260            & $+$0.016 & 0/3 & Naive\;\checkmark \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Full tabular grid (means).}
Table~\ref{tab:full-grid} reports mean clean Acc/DP/IF for every
(dataset, attack, $\alpha$) cell of the locked 540-row grid
(bold $=$ better method: higher Acc / lower DP / lower IF).
The companion Wilcoxon table (Table~\ref{tab:wilcoxon-full}) uses
\emph{seed-paired} one-sided tests ($H_1$: Naive $>$ DRO on the named
metric).

\begin{table}[t]
\centering\scriptsize
\setlength{\tabcolsep}{3pt}
\caption{Canonical $\tau{=}1$ grid means ($n{=}6$).  Source:
\texttt{results/canonical\_tau1.json}.  Under DP/Combined attack the IF
columns are near zero by construction; non-degenerate IF appears under
the IF attack.}
\label{tab:full-grid}
\resizebox{\textwidth}{!}{%
\input{auto_generated/tabular_results.tex}
}
\end{table}

\begin{table}[t]
\centering\scriptsize
\setlength{\tabcolsep}{3.5pt}
\caption{Seed-paired Wilcoxon (one-sided): percent reduction and $p$-value
for DP and IF.  $^{***}$ marks $p{<}0.05$.  Positive $\Delta$\% means DRO
is better.  Adult/DP $\alpha{=}0.1$ is the honest $5/6$ cell ($p{=}0.031$).
LSAC/DP shows negative $\Delta$DP (DRO worse) at every $\alpha$.}
\label{tab:wilcoxon-full}
\resizebox{\textwidth}{!}{%
\input{auto_generated/wilcoxon.tex}
}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\textwidth]{../figures/fig1_main_results.pdf}
\caption{Main results overview from the locked $\tau{=}1$ protocol
($n{=}6$).  Interpret together with Tables~\ref{tab:full-grid}--\ref{tab:wilcoxon-full};
the defensible method claim is Adult/Credit, $\alpha\le0.2$, DP+Combined.
Constant-predictor accuracy baselines (Adult $\approx0.7521$, Credit
$\approx0.7788$, LSAC $\approx0.9016$) bound the $\alpha\ge0.3$ regime
on Adult/Credit; LSAC stays pinned at its baseline.}
\label{fig:main-results}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.92\textwidth]{../figures/fig2_dp_reduction_heatmap.pdf}
\caption{DP reduction heatmap (Naive $-$ DRO; positive $=$ DRO better)
under the canonical protocol.  LSAC/DP cells are the known degenerate
negative (red / negative reduction); LSAC accuracy is pinned at the
constant-predictor baseline $\approx0.9016$ at every $\alpha$.}
\label{fig:dp-heatmap}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\textwidth]{../figures/fig4_significance_matrix.pdf}
\caption{Significance matrix for seed-paired Wilcoxon on DP
($p{<}0.05$).  Matches the $5/6$ and $6/6$ counts discussed in the text.
Accuracy remains above the constant-predictor baseline
(Adult $\approx0.7521$, Credit $\approx0.7788$) only at $\alpha\le0.2$
on Adult/Credit.}
\label{fig:sig-matrix}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\textwidth]{../figures/fig5_accuracy_fairness_tradeoff.pdf}
\caption{Accuracy--fairness tradeoff under the locked grid.  At
$\alpha\ge0.3$ on Adult/Credit both methods fall below the
constant-predictor accuracy baselines (Adult $\approx0.7521$, Credit
$\approx0.7788$); LSAC is pinned at its constant-predictor line
($\approx0.9016$) at every $\alpha$ under the DP attack.  We make no
method claim in the below-baseline regime.}
\label{fig:tradeoff}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\textwidth]{../figures/fig7_summary_win_rates.pdf}
\caption{Summary win rates across the canonical grid.  Not a claim of a
clean three-attack sweep: IF-attack outcomes remain \emph{mixed}
(Table~\ref{tab:wilcoxon-full}).  Win counts are restricted to the
$\alpha\le0.2$ regime where accuracy is above the constant-predictor
baselines (Adult $\approx0.7521$, Credit $\approx0.7788$); LSAC/DP is
excluded as degenerate.}
\label{fig:win-rates}
\end{figure}

\paragraph{Adversarial vs.\ random corruption (scope note).}
A historical pilot (pre-canonical protocol; not re-run under the locked
540-row settings) found that fairness-targeted PGD raised Adult DP far
more than matched-$\alpha$ random label noise.  We retain that qualitative
sanity check (adversarial $\gg$ random) but do \emph{not} treat the older
``$12$--$40\times$'' factor as a locked canonical number until a
seed-paired re-run under $\tau{=}1$, $k_{\mathrm{inner}}{=}10$, $n{=}6$
is committed.  On LSAC the DP-targeted attack remains weak in absolute
terms because clean DP is small (see \S\ref{sec:lsac-framing}).

\paragraph{k-NN graph for the IF attack (scope note).}
Canonical IF-attack rows use the default $k{=}5$ neighbourhood graph in
the attack construction.  A full $k\in\{5,10,15\}$ ablation table is
\emph{not} part of the locked 540-row science file; we therefore do not
claim $k$-insensitivity as a verified result here.  Default $k{=}5$ is
used consistently for all IF-attack cells in
\texttt{results/canonical\_tau1.json}.

\paragraph{Empirical radii (Q5).}
The bias-corrected clean-proportion formula
$\hat\pi_{j,\mathrm{clean}}=(\hat\pi_j-\alpha)/(1-2\alpha)$ is an
empirical estimate, not a theoretical guarantee.  Following Kuldeep's
Q5 we treat it as such: when the coordinated (70\%-minority) attack
is \emph{known}, the empirical $\pi_{\mathrm{clean}}$ can be tightened
from the post-attack observed proportions directly.  We retain the
closed-form as the default and add an empirical mode
(\texttt{radii\_mode='empirical'} in
\texttt{src/training/dro\_fair.py::\_compute\_radii}) for the
known-attack setting.  The locked 540-row grid uses
\texttt{radii\_mode=uniform}; the empirical path is derivation-ready
(Appendix~\ref{app:empirical-radii}) but not a second full experiment
table.  This does \emph{not} claim the paper is wrong.

\paragraph{LSAC framing (Q3).}
\label{sec:lsac-framing}
LSAC's DP is \textbf{degenerate}: the model collapses to the
constant (majority-class) predictor---accuracy $\approx0.902$, i.e.\
the $0.9016$ majority rate---at every $\alpha$, and DRO loses to Naive
on LSAC/DP at every $\alpha$ ($0/6$ seeds).  This is a \emph{negative}
result that we report openly, not a hidden failure of DRO: it is a
consequence of LSAC's near-zero clean DP, which makes the DP-targeted
attack ineffective.  By contrast, LSAC/Combined is a genuine clean win
(Wilcoxon $p{=}0.0156$ at $\alpha{=}0.1,0.3,0.4$).  The canonical
$\tau{=}1$, $k_{\mathrm{inner}}{=}10$, 6-seed grid is complete
(\textbf{540 rows}: DP+IF+Combined); Adult and Credit remain the
defensible lead, with LSAC reported as split (Combined win / DP
degeneracy / IF-attack DP loss).

\paragraph{Q7: IF $\leftrightarrow$ DP coupling (complete grid).}
The IF-attack third is complete (180 rows; cosine IF metric
non-degenerate, $\max|\mathrm{if\_clean}|\!\approx\!0.24$; full
Wilcoxon in \texttt{results/if\_wilcoxon\_summary.txt},
\texttt{results/if\_wilcoxon\_n4\_summary.md}, and
Table~\ref{tab:wilcoxon-full}).

\textbf{IF metric itself (Kuldeep Jun~30: ``if individual fairness is good
for $\alpha{=}0.3$, then we can state this clearly'').}
Under the IF \emph{attack}, seed-paired one-sided Wilcoxon on the
\textbf{IF violation} (not DP) shows DRO significantly better than Naive on
Adult and Credit at every $\alpha\in\{0.1,0.2,0.3,0.4\}$ ($6/6$,
$p{=}0.0156$).  Explicit high-$\alpha$ cells: Adult $\alpha{=}0.3$ IF
$0.0334\!\to\!0.0258$; Credit $\alpha{=}0.3$ IF $0.1212\!\to\!0.1011$.
LSAC only joins at $\alpha\in\{0.3,0.4\}$ on the IF metric
($6/6$, $p{=}0.0156$); at $\alpha\le0.2$ LSAC/IF is a loss or n.s.

\textbf{DP under the same IF attack is mixed} (coupling, not interchangeability):
DRO still lowers DP on Adult at $\alpha\le0.2$ ($6/6$, $p{=}0.0156$) and on
most Credit cells (Credit $\alpha{=}0.1$ is $4/6$, n.s.), but Adult
$\alpha{=}0.3$ \emph{loses} on DP ($\mathrm{DP}_{\mathrm{DRO}}{=}0.0241 >
\mathrm{DP}_{\mathrm{Naive}}{=}0.0227$, $1/6$, $p{=}0.8906$) even while IF
wins, and LSAC loses on DP for $\alpha\le0.3$ ($0/6$).  We therefore state clearly:
\textbf{IF-targeted robustness holds for Adult/Credit including $\alpha{=}0.3$
on the IF metric}, while DP-under-IF is not a second clean DP story.  We do
\emph{not} claim a three-attack DP sweep on every dataset.

\paragraph{Statistical significance.}
The canonical grid uses $n{=}6$ seeds (0--5).  Wilcoxon signed-rank
one-sided $p{<}0.05$ holds at $\alpha\le0.2$ on Adult and Credit under
the DP-targeted and Combined attacks (seed-paired; verified in
\texttt{results/canonical\_tau1.json}).  The full attack grid is
complete (540 rows).  Under IF attack the DP test is significant on
Adult $\alpha\le0.2$ and most Credit cells; Adult IF $\alpha{=}0.3$ and
LSAC/IF do not support a DP win (see summary file).  The IF
\emph{metric} tests are separate and support Adult/Credit through
$\alpha{=}0.4$ (above).

\begin{figure}[t]
\centering
\includegraphics[width=0.92\textwidth]{../figures/figD1_constant_predictor_accuracy.pdf}
\caption{Accuracy vs.\ $\alpha$ with the constant-predictor baseline
drawn per dataset (Adult majority $\approx0.7521$, Credit
$\approx0.7788$, LSAC $\approx0.9016$).  At $\alpha\ge0.3$ both methods
fall below the line on Adult/Credit --- the defensible method claim
remains $\alpha\le0.2$ for accuracy, even when fairness gaps favour
DRO.  LSAC stays pinned at its baseline under the DP attack.}
\label{fig:constant-predictor}
\end{figure}

\subsection{Image data (UTKFace) --- real features; mixed clean-test results}
\label{sec:results-utkface}

UTKFace uses real ResNet18 features ($N{=}23{,}705$, gender prediction,
race White/non-White as protected attribute;
\texttt{data/raw/utkface\_features.npz}).  We ran the same Fairness-Targeted
PGD protocol as tabular ($\tau{=}1$, $k_{\mathrm{inner}}{=}10$, $n{=}6$,
three attacks, five $\alpha$) into
\texttt{results/utkface\_canonical.json} (all rows
\texttt{data\_provenance{=}REAL}; 90/90 configs).

\paragraph{Honest summary (clean test metrics, matching the tabular protocol).}
Unlike Adult/Credit at $\alpha\le0.2$, clean-test DP under DP or Combined
attack is \emph{not} a low-$\alpha$ DRO sweep: many cells are mixed or
non-significant.  Significant clean DP wins for DRO appear mainly at
\textbf{high $\alpha$}: e.g.\ DP attack $\alpha{=}0.4$ (Naive DP $0.222$ vs
DRO $0.213$, $6/6$, $p{=}0.016$) and Combined $\alpha{=}0.4$ (Naive $0.144$
vs DRO $0.136$, $6/6$, $p{=}0.016$).  Under the IF \emph{attack}, clean IF
is often lower for DRO while clean DP remains mixed (coupling, as on tabular
LSAC/Adult high-$\alpha$).  Accuracy stays high ($\approx0.78$--$0.86$).
We therefore treat UTKFace as a \textbf{real image-feature pilot}: protocol
works end-to-end, but results \textbf{do not copy} the Adult/Credit
$\alpha\le0.2$ narrative.  Prior synthetic-only smoke and the ``DRO inverts
on image features'' claim remain \emph{withdrawn}.

\begin{table}[h]
\centering\small
\caption{UTKFace REAL pilot (clean-test DP; $n{=}6$).  Wins $=$ seeds where
DRO DP $<$ Naive DP.  Only $\alpha{=}0.4$ DP/Combined reach $p{<}0.05$.
Source: \texttt{results/utkface\_summary.md}.}
\label{tab:utkface-pilot}
\begin{tabular}{llcccc}
\toprule
Attack & $\alpha$ & DP Naive & DP DRO & Wins & $p$ \\
\midrule
DP & 0.1 & 0.048 & 0.051 & 1/6 & 0.969 \\
DP & 0.2 & 0.157 & 0.159 & 2/6 & 0.844 \\
DP & 0.4 & 0.222 & 0.213 & 6/6 & 0.016 \\
Combined & 0.1 & 0.031 & 0.033 & 2/6 & 0.953 \\
Combined & 0.4 & 0.144 & 0.136 & 6/6 & 0.016 \\
IF & 0.2 & 0.018 & 0.019 & 2/6 & 0.719 \\
IF & 0.4 & 0.016 & 0.016 & 4/6 & 0.422 \\
\bottomrule
\end{tabular}
\end{table}

Full cell table: \texttt{results/utkface\_summary.md}.
