ORCHESTRATOR LIVE STATUS
Updated: 2026-06-17 ~20:58

Experiments (confirmed running at high CPU):

lambda grid (PID 16334, 16+ min elapsed, 99%+ CPU)
  rows: 41/72
  current: training [14/72] α=0.1 seed=1 λ_init=0.1 lr_lambda=0.001
  (no new rows appended yet)

canonical (PID 18580, ~5 min on current invocation, 99%+ CPU)
  rows: 69/540
  current: training [70/540] adult α=0.1 seed=5 if method=dro   <-- LAST ADULT ROW
  (will auto continue to Credit + LSAC after this)

Tooling:

- Live one-shot status:
  python3 finalize_experiments.py status

- Auto-watch + auto-finalize when counts change:
  python3 watch_and_finalize.py

- Clean monitors active in this Grok session (only real completion lines will appear as chat events):
  - lambda completions
  - canonical completions / Credit/LSAC start

When you see a completion line or run the status and see higher counts:

  python3 finalize_experiments.py
  # then review + commit safe artifacts (tau1_summary, figures, pdfs, etc. -- NOT the .json while still writing)

PIDs to watch/kill only if needed:
  16334 (lambda)
  18580 (canonical)

Both runners are resume-safe.
