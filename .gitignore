# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Data
data/raw/
*.csv
*.xls
*.xlsx
*.data

# Model checkpoints (large binary files)
*.pth
*.pt

# Keep results/ and figures/ tracked — they ARE the deliverables
# results/
# figures/

# Pickle checkpoint (large, regenerable)
results/checkpoint.pkl
results/all_results.pkl

# Logs
*.log
nohup.out

# OS
.DS_Store
Thumbs.db

# Project specific
paper_text.txt
.last_checkpoint
/_archive/

# Large / retracted PDFs and dead process-id files
paper/ICML_submission.pdf
*.pid

# Temporary per-dataset result files (merged into fairness_pgd_results.json)
results/fairness_pgd_adult.json
results/fairness_pgd_credit.json
results/fairness_pgd_lsac.json

# Session zip files
session_*.zip

# Local comfort duplicate folders (for chat comfort on laptop only; NOT part of project/repo)
# These contain copies of meeting artifacts (plots, KULDEEP_DISCUSSION copy, ready message).
# Originals stay in root (KULDEEP_DISCUSSION.md) and figures/ and results/.
# Never commit these; they are purely local duplicates for user's personal use during chats.
kuldeep_meeting/
FRIEND/
friend/
FRIEND/
packages/
results/*.STALE_*

# Runtime logs / pids (keep README)
logs/*.log
logs/*.out
logs/*.lock
logs/*.pid
logs/*_paused.txt
!logs/README.md

# Caches
.pytest_cache/
**/__pycache__/

# Large local image dumps (features npz is enough)
data/raw/utkface/
data/raw/**/UTKFace/

# Offline pip wheel caches (flair2 GPU setup) — never commit these
wheelhouse/
*.whl
